package ui;

public class GameApp {

    public GameApp() {

        // fields:
            // String username;
            // int totalCoins = 0;

        // generating random coins
            // int maxCoins = 3000;
            // int minCoins = 600;
            // totalCoins = (int) (Math.random() * (maxCoins - minCoins)) + minCoins;

        // powering up a character
            // MODIFIES: this, totalCoins
            // EFFECTS: if the user has enough coins to power up a given skill,
            //              - subtract powerUpCost from totalCoins
            //              - power up skill
            //              - display "Power up successful"
            //          otherwise, display "Insufficient coins!"


        // initialize characters
            // instantiate characterList object as charactersOwned
            // instantiate charactersAvailable object

    }
}
